# Mercury #436 — Codex sync-audit: dashboard opt-in auto-refresh toggle

You are auditing a Mercury PR-candidate diff before it ships. Provide a structured PASS / NEEDS-CHANGES verdict.

## Repo

`D:/Mercury/Mercury` (Windows; PowerShell shell; main lane checkout)

## Branch

`feat/dashboard-auto-refresh-toggle` (off `develop` @ `f415b44`)

## Issue

#436 — Phase 6 dashboard: opt-in auto-refresh toggle. Closes #436 (1/4 remaining #427 v2 backlog items). Frontend-only (no Rust changes).

## Files changed

1. **NEW** `mercury-gui/src/lib/dashboardPrefs.ts` — localStorage wrapper for auto-refresh prefs (enabled + intervalMs). Try/catch around getItem/setItem (MDN: SecurityError + QuotaExceededError). Validated interval whitelist (30s / 60s / 5min).

2. **MODIFIED** `mercury-gui/src/components/GitHubDashboard.tsx` — toolbar gains an `Auto` checkbox + interval `<select>`. New useEffect sets up `setInterval` calling `refresh(false)` when enabled. Refs (`authErrorRef`, `loadingRef`) shield the interval handler from stale closure while avoiding timer-reset thrash on every loading/authError toggle. Cleanup via `clearInterval` on unmount + on toggle/interval change. Default OFF; persisted via `saveAutoRefreshPrefs`.

## Definition of Done (from the Issue)

1. Opt-in toggle in toolbar — default OFF. Persist enabled + interval in localStorage.
2. Interval selector — 30s / 60s / 5min fixed set; 60s default.
3. Polling honors guards: skip tick when `authError` set OR `loading` in flight.
4. Cleanup: `clearInterval` on unmount + on toggle/interval change.
5. TypeScript `pnpm build` clean (already verified: tsc + vite exit 0; bundle 293.85 kB).
6. Rust cargo test 55/55 stays green (already verified: exit 0).
7. Tier-1 (Tauri 2) CSP — no inline / no new external assets (pure JS/TS).
8. localStorage safety — try/catch around get + set (SecurityError + QuotaExceededError).

## Verification already done locally

- `pnpm build` exit 0 (tsc + vite, 293.85 kB bundle, 5.27s)
- `cargo test --lib --manifest-path mercury-gui/src-tauri/Cargo.toml` 55/55 pass
- React 19.1 (no `useEffectEvent` available in 19.1; ref-shielding is the documented alternative)

## What to audit

Read the actual files in the working tree (NOT staged in git for the new file). Focus on:

1. **Stale closure / interval reset thrash** — confirm refs are the right shield for `authError` + `loading` (not deps).
2. **localStorage safety** — confirm try/catch is correct + defaults are sane.
3. **Cleanup** — confirm `clearInterval` fires on unmount + on toggle/interval change. No double-timer leak.
4. **TypeScript correctness** — `AutoRefreshIntervalMs` literal-union safety. `Number.parseInt` cast is safe (select option values are typed).
5. **UX** — interval `<select>` disabled state when toggle is off; toggle label clarity.
6. **CLAUDE.md compliance**:
   - mercury-gui/ is Phase 6 GUI = Mercury-internal tooling (NOT external-project adapter); CLAUDE.md line 70 exemption from 200-LOC cap applies (used in S126 + S129 DISAGREE-cites).
   - No cherry-pick from external project; pure Mercury authorship.
7. **No regression risk**:
   - `useGitHubData` not modified — existing `refresh()` + `reqId` race-guard intact.
   - First-mount fetch behavior unchanged.
   - Manual `Refresh` button unchanged.
8. **MANDATORY RESEARCH compliance** — research was performed on React 19 useEffect cleanup + setInterval stale-closure + MDN localStorage; sources cited in Issue body.

## Output format

Reply with:

```
=== VERDICT: PASS|NEEDS-CHANGES ===

Critical: <count>
Major: <count>
Minor: <count>
Nits: <count>

<per-finding lines>
```

If PASS, briefly note the 1-2 strongest design choices that warrant carry-forward into future work.

If NEEDS-CHANGES, list each finding with file:line + severity + concrete fix suggestion.
