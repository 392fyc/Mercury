# mercury-gui is a standalone Tauri app — NOT part of the root pnpm workspace.
# This file ensures `pnpm install` run from inside this directory installs
# into mercury-gui/node_modules/ rather than the root workspace node_modules.
# Always run: pnpm install --ignore-workspace  (or cd here and pnpm install)
ignore-workspace = true
