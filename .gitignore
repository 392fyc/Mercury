# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
dist-ssr/
*.local
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Rust/Tauri
packages/gui/src-tauri/target/
packages/gui/src-tauri/gen/schemas/

# mercury-gui (Phase 6 GUI — standalone Tauri app)
mercury-gui/node_modules/
mercury-gui/src-tauri/target/
mercury-gui/dist/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Scratch / per-session ephemera (skill prompt files, audit outputs, etc.)
.tmp/

# Environment / Secrets
.env
.env.*
*.key
*.pem
credentials.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# OS
desktop.ini
$RECYCLE.BIN/

# Claude Code
CLAUDE.local.md
.claude/plans/
.claude/hooks/state/
.claude/scheduled_tasks.lock
.mercury/state/*
!.mercury/state/upstream-manifest.json
.claude/settings.local.json
CLAUDE.md

# Git worktrees (transient, managed by Main Agent)
/.worktrees/

# Mercury runtime state
.mercury/state/test-gate-attempts.json
.mercury/state/test-gate-*

# pr-flow skill state files (created per PR, cleaned in Phase 7)
.pr-flow-*
.mercury/sessions.json
.mercury/transcripts/
.mercury/backups/
.mercury/mercury.db
.mercury/mercury.db-wal
.mercury/mercury.db-shm
mercury.config.json
.claude/current-session.md
.codex/state/

# Autoresearch state (standalone mode, transient)
.research/

# mem0 test venv (Phase A)
.venv-mem0/

# voice-zh-input venv (#465)
.venv-voice/

# Python build artifacts
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# PR review agent guide (deployment-specific doc, not committed)
.mercury/docs/guides/argus-review-guide.md
# .pr_agent.toml is committed (project-scope config for Argus use_repo_settings_file)

# KB managed separately in parallel directory (D:/Mercury/Mercury_KB), never inside repo
Mercury_KB/

# OMC plugin runtime state (per-user, populated after /plugin install oh-my-claudecode)
.omc/

# playwright-mcp server output sandbox (console logs, storageState exports w/ live cookies) — never commit
.playwright-mcp/
